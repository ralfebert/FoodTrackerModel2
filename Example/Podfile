use_frameworks!

platform :ios, '10.0'
install! 'cocoapods', :disable_input_output_paths => true

target 'FoodTrackerModel2_Example' do
  pod 'FoodTrackerModel2', :path => '../'

  target 'FoodTrackerModel2_Tests' do
    inherit! :search_paths

    
  end
end
